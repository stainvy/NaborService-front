# Copie ce fichier en .env.local et adapte si besoin.
# Le back NestJS doit tourner sur http://localhost:3000 (Swagger sur /api).

# URL de l'API REST (préfixe global /v1)
VITE_API_URL=http://localhost:3000/v1

# URL du serveur Socket.io (sans le /v1)
VITE_SOCKET_URL=http://localhost:3000
